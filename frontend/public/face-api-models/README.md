Place `face-api.js` model weights in this folder for production-quality tracking.

Required model sets:

- `tiny_face_detector_model-weights_manifest.json` + shards
- `face_expression_model-weights_manifest.json` + shards
- `face_landmark_68_tiny_model-weights_manifest.json` + shards

Download source:
https://github.com/justadudewhohacks/face-api.js/tree/master/weights
